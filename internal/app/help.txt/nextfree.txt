Usage:
  dockernet [global flags] nextfree [flags] <count>

Flags:
  -g --group <name>          limit output to a group
  -gn --group-number <n>     limit output to a group number
  -n --network <name>        limit output to a network

Returns free IPs for one group or all groups. Default count is 2.